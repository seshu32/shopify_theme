<div class="container mx-auto px-6 py-12">
  <div class="max-w-4xl mx-auto">
    <h1 class="text-4xl font-bold text-[#344736] mb-8">{{ page.title }}</h1>
    
    {% if page.content != blank %}
      <div class="prose prose-lg max-w-none text-[#4b3c35]">
        {{ page.content }}
      </div>
    {% endif %}
  </div>
</div> 