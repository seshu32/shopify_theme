<!-- Seasonal Arrivals Section -->
<section class="seasonal-arrivals-section">
  <div class="seasonal-container">
    <!-- Section Header -->
    <div class="seasonal-header">
      <div class="season-indicator">
        <span class="current-season" id="currentSeason">Spring</span>
        <div class="season-animation">
          <span class="season-particle">🌸</span>
          <span class="season-particle">🍃</span>
          <span class="season-particle">☀️</span>
        </div>
      </div>
      <h2 class="seasonal-title">Nature's Calendar</h2>
      <p class="seasonal-subtitle">Fresh arrivals that dance with the seasons</p>
    </div>

    <!-- Seasonal Products Grid -->
    <div class="seasonal-grid">
      <!-- Spring Products -->
      <div class="seasonal-card spring-card" data-season="spring">
        <div class="card-season-badge">🌸 Spring</div>
        <div class="product-image">
          <img src="{{ 'Naturals_1_622x.webp' | asset_url }}" alt="Spring Harvest" loading="lazy">
          <div class="seasonal-overlay">
            <span class="seasonal-emoji">🌱</span>
          </div>
        </div>
        <div class="card-content">
          <h3>Fresh Green Collection</h3>
          <p>Young leaves & tender shoots</p>
          <div class="availability-badge">Available Now</div>
        </div>
        <div class="season-effects spring-effects">
          <span class="floating-element">🦋</span>
          <span class="floating-element">🌿</span>
        </div>
      </div>

      <!-- Summer Products -->
      <div class="seasonal-card summer-card" data-season="summer">
        <div class="card-season-badge">☀️ Summer</div>
        <div class="product-image">
          <img src="{{ 'PBR_2065.jpg' | asset_url }}" alt="Summer Harvest" loading="lazy">
          <div class="seasonal-overlay">
            <span class="seasonal-emoji">🥭</span>
          </div>
        </div>
        <div class="card-content">
          <h3>Tropical Mango Delights</h3>
          <p>Sun-kissed sweetness from wild orchards</p>
          <div class="availability-badge coming-soon">Coming June</div>
        </div>
        <div class="season-effects summer-effects">
          <span class="floating-element">🐝</span>
          <span class="floating-element">🌞</span>
        </div>
      </div>

      <!-- Monsoon Products -->
      <div class="seasonal-card monsoon-card" data-season="monsoon">
        <div class="card-season-badge">🌧️ Monsoon</div>
        <div class="product-image">
          <img src="{{ 'bbFiCfiE.jpeg' | asset_url }}" alt="Monsoon Harvest" loading="lazy">
          <div class="seasonal-overlay">
            <span class="seasonal-emoji">🍫</span>
          </div>
        </div>
        <div class="card-content">
          <h3>Wild Cocoa & Spices</h3>
          <p>Rain-blessed flavors from the earth</p>
          <div class="availability-badge">Available July-Sept</div>
        </div>
        <div class="season-effects monsoon-effects">
          <span class="floating-element">💧</span>
          <span class="floating-element">🍃</span>
        </div>
      </div>

      <!-- Winter Products -->
      <div class="seasonal-card winter-card" data-season="winter">
        <div class="card-season-badge">❄️ Winter</div>
        <div class="product-image">
          <img src="{{ 'logo.png' | asset_url }}" alt="Winter Harvest" loading="lazy">
          <div class="seasonal-overlay">
            <span class="seasonal-emoji">🌰</span>
          </div>
        </div>
        <div class="card-content">
          <h3>Nuts & Warming Spices</h3>
          <p>Nature's comfort for cold days</p>
          <div class="availability-badge">Available Dec-Feb</div>
        </div>
        <div class="season-effects winter-effects">
          <span class="floating-element">❄️</span>
          <span class="floating-element">🦌</span>
        </div>
      </div>
    </div>

    <!-- Season Navigation -->
    <div class="season-navigation">
      <button class="season-btn active" data-season="spring">
        <span class="season-icon">🌸</span>
        Spring
      </button>
      <button class="season-btn" data-season="summer">
        <span class="season-icon">☀️</span>
        Summer
      </button>
      <button class="season-btn" data-season="monsoon">
        <span class="season-icon">🌧️</span>
        Monsoon
      </button>
      <button class="season-btn" data-season="winter">
        <span class="season-icon">❄️</span>
        Winter
      </button>
    </div>


  </div>
</section>

<style>
.seasonal-arrivals-section {
  padding: 80px 20px;
  background: linear-gradient(135deg, #f8f9f7 0%, #e8f5e8 100%);
  position: relative;
  overflow: hidden;
}

.seasonal-container {
  max-width: 1200px;
  margin: 0 auto;
}

/* Header Styles */
.seasonal-header {
  text-align: center;
  margin-bottom: 60px;
  position: relative;
}

.season-indicator {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 15px;
  margin-bottom: 20px;
}

.current-season {
  font-family: 'ABCArizonaFlare', serif;
  font-size: 24px;
  color: #344736;
  font-weight: 600;
}

.season-animation {
  display: flex;
  gap: 10px;
}

.season-particle {
  font-size: 20px;
  animation: float 3s ease-in-out infinite;
  animation-delay: var(--delay, 0s);
}

.season-particle:nth-child(1) { --delay: 0s; }
.season-particle:nth-child(2) { --delay: 1s; }
.season-particle:nth-child(3) { --delay: 2s; }

.seasonal-title {
  font-family: 'ABCArizonaFlare', serif;
  font-size: 48px;
  color: #342e29;
  margin-bottom: 15px;
  font-weight: 300;
}

.seasonal-subtitle {
  font-family: 'ABCArizonaSans', sans-serif;
  font-size: 18px;
  color: #4b3c35;
  max-width: 600px;
  margin: 0 auto;
}

/* Grid Styles */
.seasonal-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: 30px;
  margin-bottom: 50px;
}

.seasonal-card {
  background: white;
  border-radius: 20px;
  overflow: hidden;
  box-shadow: 0 10px 30px rgba(52, 46, 41, 0.1);
  transition: all 0.4s ease;
  position: relative;
  cursor: pointer;
}

.seasonal-card:hover {
  transform: translateY(-10px);
  box-shadow: 0 20px 50px rgba(52, 46, 41, 0.2);
}

.card-season-badge {
  position: absolute;
  top: 15px;
  left: 15px;
  background: rgba(255, 255, 255, 0.9);
  backdrop-filter: blur(10px);
  padding: 8px 15px;
  border-radius: 20px;
  font-size: 14px;
  font-weight: 600;
  z-index: 3;
  border: 2px solid rgba(52, 71, 54, 0.2);
}

.product-image {
  position: relative;
  height: 200px;
  overflow: hidden;
}

.product-image img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform 0.4s ease;
}

.seasonal-card:hover .product-image img {
  transform: scale(1.1);
}

.seasonal-overlay {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  font-size: 40px;
  opacity: 0;
  transition: opacity 0.3s ease;
}

.seasonal-card:hover .seasonal-overlay {
  opacity: 1;
}

.card-content {
  padding: 25px;
}

.card-content h3 {
  font-family: 'ABCArizonaFlare', serif;
  font-size: 22px;
  color: #342e29;
  margin-bottom: 10px;
}

.card-content p {
  font-family: 'ABCArizonaSans', sans-serif;
  color: #4b3c35;
  font-size: 14px;
  margin-bottom: 15px;
  line-height: 1.5;
}

.availability-badge {
  display: inline-block;
  background: #b8dc99;
  color: #344736;
  padding: 6px 15px;
  border-radius: 15px;
  font-size: 12px;
  font-weight: 600;
}

.availability-badge.coming-soon {
  background: #ffcf83;
  color: #86312b;
}

/* Season Effects */
.season-effects {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  pointer-events: none;
  opacity: 0;
  transition: opacity 0.3s ease;
}

.seasonal-card:hover .season-effects {
  opacity: 1;
}

.floating-element {
  position: absolute;
  font-size: 16px;
  animation: seasonFloat 4s ease-in-out infinite;
}

.floating-element:nth-child(1) {
  top: 20%;
  left: 10%;
  animation-delay: 0s;
}

.floating-element:nth-child(2) {
  top: 60%;
  right: 15%;
  animation-delay: 2s;
}

/* Season Navigation */
.season-navigation {
  display: flex;
  justify-content: center;
  gap: 20px;
  margin-bottom: 60px;
  flex-wrap: wrap;
}

.season-btn {
  background: white;
  border: 2px solid #b8dc99;
  padding: 15px 25px;
  border-radius: 25px;
  display: flex;
  align-items: center;
  gap: 10px;
  cursor: pointer;
  transition: all 0.3s ease;
  font-family: 'ABCArizonaSans', sans-serif;
  font-weight: 600;
  color: #344736;
}

.season-btn:hover,
.season-btn.active {
  background: #b8dc99;
  transform: translateY(-2px);
  box-shadow: 0 5px 15px rgba(184, 220, 153, 0.3);
}

.season-icon {
  font-size: 18px;
}



/* Animations */
@keyframes float {
  0%, 100% { transform: translateY(0px); }
  50% { transform: translateY(-10px); }
}

@keyframes seasonFloat {
  0%, 100% { 
    transform: translateY(0px) rotate(0deg);
    opacity: 0.7;
  }
  25% { 
    transform: translateY(-15px) rotate(90deg);
    opacity: 1;
  }
  50% { 
    transform: translateY(-25px) rotate(180deg);
    opacity: 0.8;
  }
  75% { 
    transform: translateY(-15px) rotate(270deg);
    opacity: 1;
  }
}

/* Responsive Design */
@media (max-width: 768px) {
  .seasonal-title {
    font-size: 36px;
  }
  
  .seasonal-grid {
    grid-template-columns: 1fr;
    gap: 20px;
  }
  
  .season-navigation {
    justify-content: center;
    gap: 10px;
  }
  
  .season-btn {
    padding: 12px 20px;
    font-size: 14px;
  }
  
  .subscribe-form {
    flex-direction: column;
    gap: 15px;
  }
  

}
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
  // Auto-detect current season
  const currentMonth = new Date().getMonth();
  let currentSeason;
  
  if (currentMonth >= 2 && currentMonth <= 4) {
    currentSeason = 'spring';
  } else if (currentMonth >= 5 && currentMonth <= 7) {
    currentSeason = 'summer';
  } else if (currentMonth >= 8 && currentMonth <= 10) {
    currentSeason = 'monsoon';
  } else {
    currentSeason = 'winter';
  }
  
  // Update season indicator
  const seasonNames = {
    spring: 'Spring 🌸',
    summer: 'Summer ☀️',
    monsoon: 'Monsoon 🌧️',
    winter: 'Winter ❄️'
  };
  
  document.getElementById('currentSeason').textContent = seasonNames[currentSeason];
  
  // Season navigation
  const seasonBtns = document.querySelectorAll('.season-btn');
  const seasonCards = document.querySelectorAll('.seasonal-card');
  
  seasonBtns.forEach(btn => {
    btn.addEventListener('click', () => {
      const season = btn.dataset.season;
      
      // Update active button
      seasonBtns.forEach(b => b.classList.remove('active'));
      btn.classList.add('active');
      
      // Show/hide relevant cards with animation
      seasonCards.forEach(card => {
        if (card.dataset.season === season) {
          card.style.display = 'block';
          setTimeout(() => {
            card.style.opacity = '1';
            card.style.transform = 'translateY(0)';
          }, 100);
        } else {
          card.style.opacity = '0';
          card.style.transform = 'translateY(20px)';
          setTimeout(() => {
            card.style.display = 'none';
          }, 300);
        }
      });
    });
  });
  
  // Set initial active season
  const currentSeasonBtn = document.querySelector(`[data-season="${currentSeason}"]`);
  if (currentSeasonBtn) {
    currentSeasonBtn.click();
  }
  
  // Newsletter subscription
  const subscribeForm = document.querySelector('.subscribe-form');
  subscribeForm.addEventListener('submit', function(e) {
    e.preventDefault();
    const email = this.querySelector('input[type="email"]').value;
    
    // Here you would integrate with your newsletter service
    alert('Thank you for joining our seasonal journey! 🌿');
    this.reset();
  });
});
</script> 