{% comment %}
Hero Section - Main landing section with call-to-action
{% endcomment %}

<section class="section-hero">
  <div class="container">
    <div class="hero-content">
      {% if section.settings.tagline != blank %}
        <span class="hero-tagline">{{ section.settings.tagline }}</span>
      {% endif %}
      
      <h1 class="hero-title">
        {{ section.settings.title_line1 | default: 'Where You Taste' }}
        <br>
        <span class="hero-highlight">{{ section.settings.title_line2 | default: 'Into the Story' }}</span>
      </h1>
      
      {% if section.settings.description != blank %}
        <p class="hero-description">{{ section.settings.description }}</p>
      {% endif %}
      
      <div class="hero-buttons">
        {% if section.settings.primary_button_text != blank %}
          <a href="{{ section.settings.primary_button_link | default: '#' }}" class="btn btn-primary">
            {{ section.settings.primary_button_text }}
          </a>
        {% endif %}
        
        {% if section.settings.secondary_button_text != blank %}
          <a href="{{ section.settings.secondary_button_link | default: '#' }}" class="btn btn-outline">
            {{ section.settings.secondary_button_text }}
          </a>
        {% endif %}
      </div>
    </div>
    
    {% if section.settings.show_image %}
      <div class="hero-image">
        {% if section.settings.hero_image %}
          <img src="{{ section.settings.hero_image | img_url: '800x600' }}" alt="{{ section.settings.image_alt | default: 'Hero image' }}" loading="eager">
        {% else %}
          <div class="hero-image-placeholder">
            <span class="hero-emoji">{{ section.settings.placeholder_emoji | default: '🌱' }}</span>
          </div>
        {% endif %}
      </div>
    {% endif %}
  </div>
  
  {% if section.settings.show_scroll_indicator %}
    <div class="scroll-indicator">
      <div class="scroll-mouse">
        <div class="scroll-wheel"></div>
      </div>
      <span class="scroll-text">{{ section.settings.scroll_text | default: 'Scroll to explore' }}</span>
    </div>
  {% endif %}
</section>

<style>
  .section-hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    position: relative;
    overflow: hidden;
  }
  
  .section-hero::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(135deg, var(--color-dark-green) 0%, var(--color-medium-green) 50%, var(--color-dark-green) 100%);
    z-index: -2;
  }
  
  .section-hero::after {
    content: '';
    position: absolute;
    top: 20%;
    right: -10%;
    width: 400px;
    height: 400px;
    background: radial-gradient(circle, rgba(255, 207, 131, 0.1) 0%, transparent 70%);
    border-radius: 50%;
    z-index: -1;
  }
  
  .hero-content {
    text-align: center;
    z-index: 2;
    position: relative;
  }
  
  .hero-tagline {
    display: inline-block;
    font-size: 0.9rem;
    font-weight: bold;
    letter-spacing: 3px;
    text-transform: uppercase;
    color: var(--color-light-yellow);
    margin-bottom: 1.5rem;
    padding: 8px 20px;
    border: 1px solid var(--color-light-yellow);
    border-radius: 25px;
    background: rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(10px);
  }
  
  .hero-title {
    font-size: 4rem;
    font-weight: 300;
    line-height: 1.1;
    margin-bottom: 2rem;
    color: var(--color-text-light);
  }
  
  .hero-highlight {
    color: var(--color-light-yellow);
    font-weight: normal;
  }
  
  .hero-description {
    font-size: 1.2rem;
    line-height: 1.8;
    color: rgba(255, 255, 255, 0.9);
    max-width: 600px;
    margin: 0 auto 3rem;
  }
  
  .hero-buttons {
    display: flex;
    gap: 1.5rem;
    justify-content: center;
    flex-wrap: wrap;
    margin-bottom: 4rem;
  }
  
  .hero-image {
    margin-top: 3rem;
    text-align: center;
  }
  
  .hero-image img {
    max-width: 500px;
    width: 100%;
    height: auto;
    border-radius: 12px;
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
    transition: transform 0.3s ease;
  }
  
  .hero-image img:hover {
    transform: translateY(-10px);
  }
  
  .hero-image-placeholder {
    width: 400px;
    height: 300px;
    margin: 0 auto;
    background: linear-gradient(135deg, var(--color-light-green), var(--color-light-yellow));
    border-radius: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
  }
  
  .hero-emoji {
    font-size: 6rem;
    filter: drop-shadow(0 4px 8px rgba(0, 0, 0, 0.2));
  }
  
  .scroll-indicator {
    position: absolute;
    bottom: 2rem;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    color: rgba(255, 255, 255, 0.7);
    animation: bounce 2s infinite;
  }
  
  .scroll-mouse {
    width: 24px;
    height: 36px;
    border: 2px solid rgba(255, 255, 255, 0.7);
    border-radius: 12px;
    position: relative;
  }
  
  .scroll-wheel {
    width: 4px;
    height: 8px;
    background: rgba(255, 255, 255, 0.7);
    border-radius: 2px;
    position: absolute;
    top: 6px;
    left: 50%;
    transform: translateX(-50%);
    animation: scroll 2s infinite;
  }
  
  .scroll-text {
    font-size: 0.8rem;
    text-transform: uppercase;
    letter-spacing: 1px;
  }
  
  @keyframes bounce {
    0%, 20%, 50%, 80%, 100% {
      transform: translateX(-50%) translateY(0);
    }
    40% {
      transform: translateX(-50%) translateY(-10px);
    }
    60% {
      transform: translateX(-50%) translateY(-5px);
    }
  }
  
  @keyframes scroll {
    0% {
      top: 6px;
      opacity: 1;
    }
    50% {
      top: 12px;
      opacity: 0.5;
    }
    100% {
      top: 18px;
      opacity: 0;
    }
  }
  
  @media (max-width: 768px) {
    .hero-title {
      font-size: 2.5rem;
    }
    
    .hero-description {
      font-size: 1rem;
    }
    
    .hero-buttons {
      flex-direction: column;
      align-items: center;
    }
    
    .hero-image-placeholder {
      width: 300px;
      height: 200px;
    }
    
    .hero-emoji {
      font-size: 4rem;
    }
  }
  
  @media (max-width: 480px) {
    .hero-title {
      font-size: 2rem;
    }
    
    .hero-tagline {
      font-size: 0.8rem;
      letter-spacing: 2px;
      padding: 6px 16px;
    }
    
    .section-hero {
      padding: 2rem 0;
    }
  }
</style>

{% schema %}
{
  "name": "Hero Section",
  "settings": [
    {
      "type": "text",
      "id": "tagline",
      "label": "Tagline",
      "default": "SUSTAINABLE & ORGANIC FARMING"
    },
    {
      "type": "text",
      "id": "title_line1",
      "label": "Title Line 1",
      "default": "Where You Taste"
    },
    {
      "type": "text",
      "id": "title_line2",
      "label": "Title Line 2 (Highlighted)",
      "default": "Into the Story"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Description",
      "default": "Experience the authentic flavors of farm-fresh produce, grown with care and harvested at peak ripeness. Every bite tells the story of sustainable farming and passionate dedication."
    },
    {
      "type": "text",
      "id": "primary_button_text",
      "label": "Primary Button Text",
      "default": "Shop Now"
    },
    {
      "type": "url",
      "id": "primary_button_link",
      "label": "Primary Button Link",
      "default": "/collections"
    },
    {
      "type": "text",
      "id": "secondary_button_text",
      "label": "Secondary Button Text",
      "default": "Learn More"
    },
    {
      "type": "url",
      "id": "secondary_button_link",
      "label": "Secondary Button Link",
      "default": "/about"
    },
    {
      "type": "checkbox",
      "id": "show_image",
      "label": "Show hero image",
      "default": true
    },
    {
      "type": "image_picker",
      "id": "hero_image",
      "label": "Hero Image"
    },
    {
      "type": "text",
      "id": "image_alt",
      "label": "Image Alt Text",
      "default": "Sustainable farming hero image"
    },
    {
      "type": "text",
      "id": "placeholder_emoji",
      "label": "Placeholder Emoji (if no image)",
      "default": "🌱"
    },
    {
      "type": "checkbox",
      "id": "show_scroll_indicator",
      "label": "Show scroll indicator",
      "default": true
    },
    {
      "type": "text",
      "id": "scroll_text",
      "label": "Scroll Indicator Text",
      "default": "Scroll to explore"
    }
  ],
  "presets": [
    {
      "name": "Hero Section"
    }
  ]
}
{% endschema %} 